kind: PersistentVolume
apiVersion: v1
metadata:
  name: slurm-prod--mysql-data
  labels:
    type: local
spec:
  storageClassName: slurm-prod--mysql-data
  capacity:
    storage: 10000Gi
  accessModes:
    - ReadWriteOnce
  #hostPath:
  local:
    path: /scratch/slurm/database/
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - os-dev01
